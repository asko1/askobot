const config = {
    "token": "your_token_here",
    // defaults for server configs
    "defaultSettings" : {
        "prefix": "!",
        "modLogChannel": "",
    },
    // color for embeds
    "color": "3DF5FF",
};

module.exports = config;